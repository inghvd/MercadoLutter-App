<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link rel="stylesheet" href="/css/style.css"> 
  <title><%= typeof title !== 'undefined' ? title : 'MercadoLutter' %></title>
</head>
<body class="bg-light">

<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
  <div class="container">
    <a class="navbar-brand" href="/">MercadoLutter</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <li class="nav-item">
          <a class="nav-link" href="/catalogo">Catálogo</a>
        </li>

        <%# --- Lógica de botones para usuarios logueados --- %>
        <% if (usuario) { %>

          <!-- 1. "MI PANEL" PARA TODOS LOS USUARIOS LOGUEADOS (CLIENTE Y ADMIN) -->
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" id="panelUsuarioDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Mi Panel
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="panelUsuarioDropdown">
              <li><a class="dropdown-item" href="/usuario/productos">Mis Productos</a></li>
              <li><a class="dropdown-item" href="/usuario/crear-producto">Publicar Producto</a></li>
              <li><hr class="dropdown-divider"></li>
              <li><a class="dropdown-item" href="/usuario/perfil">Editar Mi Perfil</a></li>
            </ul>
          </li>

          <!-- 2. BOTÓN EXTRA QUE SOLO VE EL ADMIN -->
          <% if (usuario.rol === 'admin') { %>
            <li class="nav-item">
              <a class="nav-link fw-bold text-warning" href="/admin/dashboard">Panel Admin</a>
            </li>
          <% } %>

          <!-- 3. CERRAR SESIÓN PARA TODOS -->
          <li class="nav-item">
            <a class="nav-link" href="/auth/logout">Cerrar Sesión</a>
          </li>

        <% } else { %>
          <%# --- Botones para VISITANTES (no logueados) --- %>
          <li class="nav-item">
            <a class="nav-link" href="/auth/login">Iniciar Sesión</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/auth/registro">Registrarse</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>

<main class="container mt-4">
